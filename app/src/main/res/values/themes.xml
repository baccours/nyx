<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Plain AppCompat theme (no Material Components dependency). Every color below
         is a @color/color_* alias, so values-night/colors.xml is what actually switches
         the app between light and dark - this one file covers both. -->
    <style name="Theme.Nyx" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_container</item>
        <item name="colorAccent">@color/color_primary</item>
        <item name="colorControlActivated">@color/color_primary</item>
        <item name="android:colorBackground">@color/color_background</item>
        <item name="android:windowBackground">@color/color_background</item>
        <item name="android:statusBarColor">@color/color_primary_container</item>
        <item name="android:navigationBarColor">@color/color_background</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">@bool/is_light_bars</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">@bool/is_light_bars</item>
    </style>

    <style name="TextAppearance.Nyx.ToolbarTitle" parent="TextAppearance.AppCompat.Title">
        <item name="android:textStyle">bold</item>
        <item name="android:letterSpacing">0.09</item>
        <item name="android:textColor">@color/color_on_primary_container</item>
    </style>
</resources>
